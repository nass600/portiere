#!/usr/bin/env php
<?php

use Nass600\Tool\Application;
use Nass600\Command\VhostCreateCommand;

$app = new Application('Vhost Builder', '1.0.0');
$app->add(
    new VhostCreateCommand()
);
$app->run();